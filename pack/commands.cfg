define command {
       command_name	check_forms
       command_line	$PLUGINSDIR$/check_http -H $HOSTADDRESS$ -u "/forms/" -p $_HOSTFORMS_LISTENER_PORT$ -r "up and running"
}

define command {
       command_name	check_forms_sessions_average_memorye_usage
       command_line	$PLUGINSDIR$/check_snmp_process.pl -H $HOSTADDRESS$ -C $_HOSTSNMPCOMMUNITYREAD$ -n frmweb -m $_HOSTFORMS_AVERAGE_MEM_WARN$,$_HOSTFORMS_AVERAGE_MEM_CRIT$ -a
}

define command {
       command_name	check_forms_sessions_max_memory_usage
       command_line	$PLUGINSDIR$/check_snmp_process.pl -H $HOSTADDRESS$ -C $_HOSTSNMPCOMMUNITYREAD$ -n frmweb -m $_HOSTFORMS_MAX_MEM_WARN$,$_HOSTFORMS_MAX_MEM_CRIT$
}

define command {
       command_name	check_forms_sessions
       command_line	$PLUGINSDIR$/check_snmp_process.pl -H $HOSTADDRESS$ -C $_HOSTSNMPCOMMUNITYREAD$ -n frmweb -w $_HOSTFORMS_MIN_WARN$,$_HOSTFORMS_MAX_WARN$ -c $_HOSTFORMS_MIN_CRIT$,$_HOSTFORMS_MAX_CRIT$
}
